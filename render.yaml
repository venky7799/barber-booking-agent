services:
  - type: web
    name: barber-booking-agent
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node serve.js
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: NODE_ENV
        value: production
